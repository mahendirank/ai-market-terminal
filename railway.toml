[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "uvicorn dashboard_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/macro"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
